# TSChain
Blockchain made in TypeScript

## Installation
Must have `node` and `npm` installed.

In root directory, run `npm install` to install dependencies.

Then run `npm run build` to build the Typescript.

Finally, run `npm run start` to run the program.
